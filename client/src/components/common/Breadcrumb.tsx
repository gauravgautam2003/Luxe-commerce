import React from 'react';

const Breadcrumb = ({ items }) => {
    return (
        <nav className="breadcrumb">
            {/* Breadcrumb content */}
        </nav>
    );
};

export default Breadcrumb;
