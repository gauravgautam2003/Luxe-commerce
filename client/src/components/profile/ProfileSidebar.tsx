import React from 'react';

const ProfileSidebar = ({ onMenuChange }) => {
    return (
        <aside className="profile-sidebar">
            {/* Profile sidebar content */}
        </aside>
    );
};

export default ProfileSidebar;
