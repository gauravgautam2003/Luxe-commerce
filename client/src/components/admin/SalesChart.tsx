import React from 'react';

const SalesChart = ({ data }) => {
    return (
        <div className="sales-chart">
            {/* Sales chart content */}
        </div>
    );
};

export default SalesChart;
