import React from 'react';

const HeroSection = () => {
    return (
        <section className="hero-section">
            {/* Hero section content */}
        </section>
    );
};

export default HeroSection;
