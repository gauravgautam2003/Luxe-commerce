import React from 'react';

const OrderCard = ({ order }) => {
    return (
        <div className="order-card">
            {/* Order card content */}
        </div>
    );
};

export default OrderCard;
